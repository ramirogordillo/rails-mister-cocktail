<div class="container">
  <h1><%= @cocktail.name %>:</h1>
  <ul><strong>Ingredients:</strong>
  <% @cocktail.doses.each do |dose| %>
    <li>
      <em><%= dose.ingredient.name %></em>
      <%=  ": " %>
      <%= dose.description %>
      <%= link_to "🗑", dose_path(dose), method: :delete %>
    </li>
  <% end %>
  <p><%= link_to "Add Ingredients/Doses", new_cocktail_dose_path(@cocktail.id) %><p>
  <p><%= link_to "Back to all cocktails", root_path %><p>
</div>
